create 2 boards(10x10) array(player view cpu view)
-labeled a-j; 1-9
-write a function to display the array with the labels

void placeship(char ship, int spaces);
-roll for orientation(horizontal/vertical)
-roll for valid spot dependant on spaces
-check if all spaces after the spot are 
available. if so, place the chars, if not reroll everything

b4 == 4b

